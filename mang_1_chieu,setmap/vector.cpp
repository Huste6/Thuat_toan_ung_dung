#include<bits/stdc++.h>
using namespace std;
//pair: cap
int main(){
    /*vector<int> v;//v(n)
    v.push_back(100);
    v.push_back(200);
    v.push_back(300);
    cout<<v.size()<<endl;
    for(int i=0;i<v.size();i++){
        cout<<v[i]<<' ';
    }
    cout<<endl;
    for(int x:v) cout<<x<<' ';
    cout<<endl;
    //rend begin_____rbegin end
    //       1  _____   5    
    vector<int>v={1,2,3,4,5};
    vector<int>::iterator it=v.begin();
    cout<< *it <<endl;
    ++it;
    cout<<*it<<endl;
    --it;
    for(it=v.begin();it!=v.end();it++){
        cout<<*it<<' ';
    }
    cout<<endl;
    v.insert(v.begin()+2,100);
    v.erase(v.begin()+2);
    v.pop_back();//xoa phan tu cuoi cung
    for(auto it:v){
        cout<<it<<' ';
    }
    cout<<endl;
    */
   int n;cin>>n;
   pair<int,int> a[m];
   for(int i=0;i<n;i++){
    cin>>a[i].first>>' '>>a[i].second;
   }
}